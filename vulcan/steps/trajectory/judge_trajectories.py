"""Trajectory verification step.

- Registered as "judge_trajectories"
- Uses typed exceptions from vulcan.core.exceptions
- Imports prompts from vulcan.prompts.judge_trajectories
- Routes: _judge_tools_only, _judge_with_user_and_policy, _judge_with_user
- FC vs thinking branching (thinking=True → tool_specs NOT passed separately,
  they are embedded in the conversation's system prompt)
"""

from __future__ import annotations

import json
import traceback

from vulcan.steps import register_step
from vulcan.steps.base import BaseStep
from vulcan.core.agent import Agent
from vulcan.core.parsers import TagParser
from vulcan.core.exceptions import ParseError
from vulcan.prompts.judge_trajectories import (
    STATIC_MT_JUDGE_SYSTEM, STATIC_MT_JUDGE_USER,
    STATIC_MT_THINKING_JUDGE_SYSTEM, STATIC_MT_THINKING_JUDGE_USER,
    DYNAMIC_JUDGE_SYSTEM, DYNAMIC_JUDGE_USER,
    DYNAMIC_THINKING_JUDGE_SYSTEM, DYNAMIC_THINKING_JUDGE_USER,
    DYNAMIC_NO_POLICY_JUDGE_SYSTEM, DYNAMIC_NO_POLICY_JUDGE_USER,
    SINGLE_TURN_JUDGE_SYSTEM, SINGLE_TURN_JUDGE_USER,
    STATIC_MT_PROMPTING_JUDGE_SYSTEM, STATIC_MT_PROMPTING_JUDGE_USER,
    DYNAMIC_PROMPTING_JUDGE_SYSTEM, DYNAMIC_PROMPTING_JUDGE_USER,
    DYNAMIC_NO_POLICY_PROMPTING_JUDGE_SYSTEM, DYNAMIC_NO_POLICY_PROMPTING_JUDGE_USER,
)


@register_step("judge_trajectories")
class JudgeTrajectoriesStep(BaseStep):
    """Verify generated trajectories using an LLM judge.

    Reads from inp:
        conversation   - trajectory produced by generate_trajectories
        mode            - "class_multi_turn" (default) or other
        category_name   - used to look up environment_type and tool specs
        thinking_mode   - True if trajectory was generated by thinking model
        ground_truth_response  - (single-turn only)
        mock_data_example_all_api - (single-turn only)

    Writes to inp:
        judgment        - parsed judgment dict (score, task_completed, etc.)
    """

    def __init__(self, config, llm, env):
        super().__init__(config, llm, env)
        # BaseStep already resolves steps.judge_trajectories.model -> defaults.model,
        # falling back to "" so the configured provider's own default applies.
        # Never hardcode a vendor model here: it would be sent verbatim to
        # whichever provider is configured.
        self.judge_agent = Agent(
            "judge", "", llm,
            model=self.model or None, temperature=0, max_tokens=self.max_tokens,
            thinking_model=False,
            output_parser=TagParser.make_parser("out_judgment"),
        )

    async def run(self, inp: dict, progress=None) -> dict:
        try:
            mode = inp.get("mode", "class_multi_turn")
            cat = inp.get("category_name", "")
            environment_type = self.env.get_environment_type(cat)

            if mode == "class_multi_turn":
                if environment_type == "with_user_and_policy":
                    await self._judge_with_user_and_policy(inp, cat, progress)
                elif environment_type == "with_user":
                    await self._judge_with_user(inp, cat, progress)
                else:
                    await self._judge_tools_only(inp, cat, progress)
            else:
                await self._verify_single_turn(inp, progress)

        except Exception:
            traceback.print_exc()

        return inp

    # ── Static verification ──────────────────────────────────────────────────

    def _resolve_style(self, inp: dict) -> str:
        """Determine the trajectory generation style: 'native' | 'reasoning' | 'tagged'.

        Priority:
          1. Explicit per-record ``tool_call_format`` (stamped by generate_trajectories).
          2. ``tool_call_format`` override in this step's config.
          3. ``thinking_mode`` flag -> "thinking".
          4. Heuristic fallback for already-generated tagged trajectories
             (which predate the style stamp): the tagged setup embeds tools
             in the system prompt and uses <tool_call>/<answer> text markers.
          5. Default "native".

        Records written by generate_trajectories always carry the stamp and take
        step 1; the later steps only matter for hand-assembled input.
        """
        style = inp.get("tool_call_format") or self.step_config.get("tool_call_format")
        if style:
            return style
        if inp.get("thinking_mode", False):
            return "reasoning"
        conv = inp.get("conversation", [])
        if isinstance(conv, list) and conv and isinstance(conv[0], dict):
            sys_content = conv[0].get("content", "") or ""
            if "<answer>" in sys_content and "<tool_call>" in sys_content:
                return "tagged"
        return "native"

    async def _judge_tools_only(self, inp: dict, cat: str, progress) -> None:
        """Verify static environment trajectory."""
        conv = inp.get("conversation", [])
        style = self._resolve_style(inp)

        if style == "tagged":
            # Prompting: tool specs embedded in conv's system prompt (<tools> tags)
            msgs = [
                {"role": "system", "content": STATIC_MT_PROMPTING_JUDGE_SYSTEM},
                {"role": "user", "content": STATIC_MT_PROMPTING_JUDGE_USER.format(CONV=conv)},
            ]
        elif style == "reasoning":
            # Thinking: tool specs already embedded in conv's system prompt (<tool> tags)
            msgs = [
                {"role": "system", "content": STATIC_MT_THINKING_JUDGE_SYSTEM},
                {"role": "user", "content": STATIC_MT_THINKING_JUDGE_USER.format(CONV=conv)},
            ]
        else:
            # FC: tool specs not in conv — pass separately
            tool_specs = self._get_api_specs_str(cat)
            msgs = [
                {"role": "system", "content": STATIC_MT_JUDGE_SYSTEM},
                {"role": "user", "content": STATIC_MT_JUDGE_USER.format(CONV=conv, TOOL_SPECS=tool_specs)},
            ]

        res = await self.judge_agent.run(msgs, progress)
        inp["judgment"] = self._parse_judgment(res)

    # ── Dynamic verification (policy-governed) ───────────────────────────────

    async def _judge_with_user_and_policy(self, inp: dict, cat: str, progress) -> None:
        """Verify dynamic environment trajectory (with policy)."""
        conv = inp.get("conversation", [])
        style = self._resolve_style(inp)

        if style == "tagged":
            # Prompting: tool specs + policy embedded in conv's system prompt
            judge_msgs = [
                {"role": "system", "content": DYNAMIC_PROMPTING_JUDGE_SYSTEM.format(DOMAIN_NAME=cat)},
                {"role": "user", "content": DYNAMIC_PROMPTING_JUDGE_USER.format(
                    DOMAIN_NAME=cat, CONV=conv,
                )},
            ]
        elif style == "reasoning":
            # Thinking: tool specs in conv's system prompt
            judge_msgs = [
                {"role": "system", "content": DYNAMIC_THINKING_JUDGE_SYSTEM},
                {"role": "user", "content": DYNAMIC_THINKING_JUDGE_USER.format(
                    DOMAIN_NAME=cat, CONV=conv,
                )},
            ]
        else:
            # FC: tool specs not in conv
            tool_specs = self._get_api_specs_str(cat)
            judge_msgs = [
                {"role": "system", "content": DYNAMIC_JUDGE_SYSTEM},
                {"role": "user", "content": DYNAMIC_JUDGE_USER.format(
                    DOMAIN_NAME=cat, CONV=conv, TOOL_SPECS=tool_specs,
                )},
            ]

        judge_res = await self.judge_agent.run(judge_msgs, progress)
        inp["judgment"] = self._parse_judgment(judge_res)

    # ── Vita verification (no policy) ────────────────────────────────────────

    async def _judge_with_user(self, inp: dict, cat: str, progress) -> None:
        """Judge a `with_user` trajectory — simulated user, no policy."""
        conv = inp.get("conversation", [])
        style = self._resolve_style(inp)

        if style == "tagged":
            # Prompting: tool specs embedded in conv's system prompt
            judge_msgs = [
                {"role": "system", "content": DYNAMIC_NO_POLICY_PROMPTING_JUDGE_SYSTEM.format(DOMAIN_NAME=cat)},
                {"role": "user", "content": DYNAMIC_NO_POLICY_PROMPTING_JUDGE_USER.format(
                    DOMAIN_NAME=cat, CONV=conv,
                )},
            ]
        elif style == "reasoning":
            # Thinking: tool specs in conv's system prompt
            judge_msgs = [
                {"role": "system", "content": STATIC_MT_THINKING_JUDGE_SYSTEM},
                {"role": "user", "content": STATIC_MT_THINKING_JUDGE_USER.format(CONV=conv)},
            ]
        else:
            # FC: tool specs not in conv
            tool_specs = self._get_api_specs_str(cat)
            judge_msgs = [
                {"role": "system", "content": DYNAMIC_NO_POLICY_JUDGE_SYSTEM},
                {"role": "user", "content": DYNAMIC_NO_POLICY_JUDGE_USER.format(
                    DOMAIN_NAME=cat, CONV=conv, TOOL_SPECS=tool_specs,
                )},
            ]

        judge_res = await self.judge_agent.run(judge_msgs, progress)
        inp["judgment"] = self._parse_judgment(judge_res)

    # ── Single-turn verification ─────────────────────────────────────────────

    async def _verify_single_turn(self, inp: dict, progress) -> None:
        """Verify single-turn trajectory."""
        conv = inp.get("conversation", [])
        gt_answer = inp.get("ground_truth_response", "")
        api_examples = inp.get("mock_data_example_all_api", "")

        msgs = [
            {"role": "system", "content": SINGLE_TURN_JUDGE_SYSTEM},
            {"role": "user", "content": SINGLE_TURN_JUDGE_USER.format(
                CONV=conv, GT_ANSWER=gt_answer, API_IO_EXAMPLES=api_examples,
            )},
        ]
        res = await self.judge_agent.run(msgs, progress)
        inp["judgment"] = self._parse_judgment(res)

    # ── Helpers ──────────────────────────────────────────────────────────────

    def _get_api_specs_str(self, cat: str) -> str:
        """Return JSON string of tool specs for a category."""
        api_specs = self.env.get_api_catalog(cat)
        specs = [a["function"] for a in api_specs]
        # Add transfer tool for dynamic domains
        if self.env.get_environment_type(cat) in ("with_user_and_policy", "with_user"):
            specs.append(self.env.TRANSFER_TO_HUMAN_API)
        return json.dumps(specs, indent=2)

    def _parse_judgment(self, res: dict) -> dict | str | None:
        """Parse judgment from agent response.

        Tries parsed_content first (from output_parser), then falls back to
        direct tag extraction from raw content.
        """
        parsed = res.get("parsed_content")
        if parsed and isinstance(parsed, list) and parsed:
            try:
                return json.loads(parsed[0]) if isinstance(parsed[0], str) else parsed[0]
            except (json.JSONDecodeError, TypeError):
                pass

        raw = TagParser.extract_first("out_judgment", res.get("content", ""))
        if raw:
            try:
                return json.loads(raw)
            except json.JSONDecodeError:
                return raw

        return None

